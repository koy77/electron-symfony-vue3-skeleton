[package]
name = "coding_ui_tauri"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
# Tauri dependency with broad API features enabled
tauri = { version = "^1.4", features = [] }

[build-dependencies]
tauri-build = { version = "^1.4", features = [] }

