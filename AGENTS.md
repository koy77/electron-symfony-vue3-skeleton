# Agent Guidelines

## Global Rules
- For any single file you modify, consolidate the changes into one editing operation instead of multiple incremental edits. This helps keep updates atomic and easier to review.
