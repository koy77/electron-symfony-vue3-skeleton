# Electron Desktop Application Configuration
# This app runs as an Electron desktop window by default
# The Vue frontend is embedded in the Electron window

# Database Configuration
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=!ChangeMe!

# Symfony Configuration
APP_SECRET=changeme_generate_a_real_secret_key
APP_ENV=dev

# Display for Electron (Linux)
# Set to your X11 display (usually :0)
DISPLAY=:0
